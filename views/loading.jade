extends base

block vars
  - var title = "Загрузка..."

block content
  h1= message
  p Подождите, пожалуйста...

  script(src="/socket.io/socket.io.js")
  script.
    socket = io.connect();
    socket.on('roomIDReturned', function onRoomFound(roomID) {
      window.location.replace('/id/' + roomID);
    });
    socket.emit('#{eventName}');